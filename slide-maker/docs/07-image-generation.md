# AI 圖片生成流程

## 概述

AI 圖片生成是簡報製作中提升視覺品質的關鍵環節。本文件定義了模型選擇、圖片規劃策略、Prompt 工程、浮水印處理、成本估算，以及與 Gemini GenImage 技能的整合方式。

---

## 模型選擇

### 預設模型：Gemini 2.5 Flash Image

```
模型：gemini-2.5-flash-preview-native-audio-dialog
成本：~$0.008 / 張
速度：快（3-8 秒）
品質：中高
```

✅ 適用：一般簡報背景圖、概念插圖、裝飾性圖片、大量批次生成

### 進階模型：Gemini 3 Pro Image

```
模型：gemini-3-pro-image (如可用)
成本：較高 / 速度：較慢（10-20 秒） / 品質：高
```

✅ 適用：需要精細細節的主視覺、品牌關鍵圖片、高解析度輸出

### 選擇決策

```
需求是否為關鍵主視覺？
  ├─ 是 → Gemini 3 Pro Image
  └─ 否 → Gemini 2.5 Flash（已足夠且成本低）
```

💡 大多數簡報用 Gemini 2.5 Flash 就夠了

---

## SDK 規格

### 使用 @google/genai（新版 SDK）

```javascript
// ✅ 正確：使用新版 SDK
import { GoogleGenAI } from "@google/genai";
const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
const response = await ai.models.generateContent({
  model: "gemini-2.5-flash-preview-native-audio-dialog",
  contents: [{ text: "prompt here" }],
  config: { responseModalities: ["image", "text"] }
});
```

```javascript
// ❌ 錯誤：舊版 SDK 已棄用
import { GoogleGenerativeAI } from "@google/generative-ai";
```

⚠️ `@google/generative-ai` 已被 `@google/genai` 取代，所有新開發必須使用新版。

---

## 圖片規劃（宏觀圖片規劃）

在生成圖片之前，先做全域規劃，決定整份簡報需要哪些圖片。

### 七大圖片類型

#### 1. Background Images（貫穿底圖）

用途：投影片背景，貫穿多頁。低對比、不搶焦點、可加半透明遮罩。通常 1-3 張。

```
Prompt 範本："Abstract geometric pattern in soft blue tones,
minimal and clean, suitable as presentation background, low contrast"
```

⚠️ 背景圖不應有明確主體，否則會與前景文字衝突

#### 2. Hero Images（主視覺）

用途：封面或重點頁面的主圖。高品質、視覺衝擊力強。通常 1-2 張。

```
Prompt 範本："A futuristic smart city at sunset, aerial view,
clean architecture, warm golden light, photorealistic, 16:9 aspect ratio"
```

✅ Hero Image 值得花較多時間調整 Prompt，甚至使用進階模型

#### 3. Architecture Diagrams（架構圖）

用途：展示系統架構、流程、組織結構。

```
Prompt 範本："Clean technical architecture diagram showing
microservices connected by arrows, flat design, white background"
```

⚠️ AI 生成的架構圖文字常不準確，建議用 AI 生成底圖風格，再疊加正確文字

#### 4. Concept Illustrations（概念圖）

用途：將抽象概念視覺化。象徵性、易理解、風格統一。

```
Prompt 範本："Isometric illustration of cloud computing concept,
showing data flowing between devices and cloud servers, modern flat design"
```

#### 5. Demo Screenshots（展示截圖）

用途：展示產品介面、操作流程。💡 通常直接截取實際產品畫面。
⚠️ 產品還在開發中時，可用 AI 生成 Mockup。

#### 6. Comparison Charts（對比圖）

用途：A vs B 的視覺對比。明確的分隔、統一格式。

```
Prompt 範本："Split screen comparison infographic,
left side in red tones, right side in green tones, clean flat design"
```

#### 7. Scenario Illustrations（場景圖）

用途：描繪使用場景、情境故事。包含人物、場景、動作。

```
Prompt 範本："Business team collaborating in a modern office,
looking at data dashboard, warm natural lighting, candid photography style"
```

---

## Prompt 工程最佳實踐

### 結構化 Prompt 模板

```
[主體描述] + [風格指定] + [色調指定] + [構圖指定] + [品質修飾]

範例：
"A developer working on code at night         ← 主體
 in digital illustration style                ← 風格
 with deep blue and neon green color palette   ← 色調
 medium shot, slightly elevated angle          ← 構圖
 high detail, professional quality"            ← 品質
```

### 撰寫原則

```
✅ 具體描述（"three people in a meeting room" 比 "people meeting" 好）
✅ 指定風格（"flat design" / "photorealistic" / "watercolor"）
✅ 指定色調（呼應簡報主題色）、指定比例（"16:9 aspect ratio"）
✅ 排除不要的元素（"no text, no watermark"）

❌ 模糊描述（"a nice picture about technology"）
❌ 過長 Prompt（超過 200 字效果反而下降）
❌ 矛盾指令（"photorealistic watercolor painting"）
```

### 風格一致性

同一份簡報所有 AI 生成圖片必須統一風格詞彙：

```
✅ 全部使用 "flat design illustration, minimal, clean lines"
❌ 第 1 張 "photorealistic"，第 3 張 "watercolor"，第 5 張 "pixel art"
```

💡 在圖片規劃階段就定好風格關鍵字，後續所有 Prompt 都使用相同的風格前綴

---

## 浮水印處理

Gemini 生成的圖片可能帶有 SynthID 浮水印，使用 GeminiWatermarkTool 處理：

```
流程：生成圖片 → 檢查浮水印 → GeminiWatermarkTool 處理 → 驗證品質
```

⚠️ 浮水印處理可能輕微影響圖片品質

---

## 與 /Gemini-GenImage 的整合

```
Step 1: 圖片規劃（決定需要哪些圖片）
Step 2: 撰寫 Prompt（每張圖片一個）
Step 3: 呼叫 /Gemini-GenImage Skill（傳入 Prompt、指定模型、指定輸出路徑）
Step 4: 檢查生成結果（品質？風格一致？）
  ├─ 不滿意 → 調整 Prompt，回到 Step 3
  └─ 滿意 → Step 5: 整合到投影片
```

### 批次生成策略

```
✅ 先生成 1 張測試風格，確認後再批次生成其餘
✅ 優先生成 Hero Image（最重要的先做）
❌ 一次生成所有圖片（風格不一致風險高）
❌ 沒有測試就批次生成（浪費 API 成本）
```

---

## 成本估算

### 典型簡報的圖片成本

| 簡報類型 | 頁數 | AI 生成圖片數 | 預估成本 |
|---------|------|-------------|---------|
| 短簡報 | 5-10 頁 | 2-4 張 | $0.016 - $0.032 |
| 中型簡報 | 15-20 頁 | 5-8 張 | $0.040 - $0.064 |
| 大型簡報 | 30+ 頁 | 10-15 張 | $0.080 - $0.120 |

### 成本控制建議

```
✅ 不是每張投影片都需要 AI 生成圖片（純數據頁用圖表就好）
✅ 共用背景圖減少生成數量（1 張可用 5-10 頁）
✅ 每張圖最多重試 2 次，超過代表 Prompt 需要重寫

💡 以 Gemini 2.5 Flash 計算：$1 USD ≈ 125 張圖片
```

---

## 品質檢查清單

- [ ] 圖片主體清晰，沒有變形或異常？
- [ ] 色調與簡報主題一致？
- [ ] 比例正確（16:9 或指定比例）？
- [ ] 沒有不當文字或亂碼？
- [ ] 與同份簡報其他圖片風格一致？
- [ ] 解析度足夠（至少 1920x1080）？
- [ ] 浮水印已處理？

---

## 常見問題排除

### AI 生成的文字不正確

Prompt 中不要要求精確文字，改用 "labeled boxes with placeholder text"，然後用圖片編輯工具疊加正確文字。

### 風格不一致

建立風格 Prompt 前綴，所有圖片共用。前綴範例："flat design, minimal, blue tone, clean lines, "，每張圖只改後半段的主體描述。

### 人物生成問題

```
✅ 使用中遠景，避免特寫
✅ 人物作為背景元素，不作為主體
✅ 用剪影或插畫風格取代寫實風格
```
